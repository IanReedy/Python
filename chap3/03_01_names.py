"""
I create a list of names called `names`, containing 'Dennis', 'Sam', 'Colin', and 'Declan'. 
Then, I use a for loop to iterate over each name in the list, printing each one with the first letter capitalized using the `title()` method.
"""
names = ['Dennis', 'Sam', 'Colin', 'Declan']
for name in names:
    print(name.title())
